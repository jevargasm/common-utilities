plugins {
    id 'java'
    id 'pl.allegro.tech.build.axion-release' version '1.10.0'
}

group 'com.samples.java'

scmVersion {
    tag {
        prefix = 'common-utilities'
    }

    repository {

    }
}

version scmVersion.version

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

task loadGitHubToken {
    scmVersion.repository.customUsername = 'jevargasm'
    scmVersion.repository.customPassword  = 'Agamenon23'
}